const config = {
  status: 999,
};

/**
 * 定义全局reducer函数
 * @param {state} state
 * @param {actiom} action
 * @returns
 */
const configReducer = (state = config, action) => {
  return state;
};
export default configReducer;
