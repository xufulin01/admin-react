const departmentState = {
  department: "部门",
};

/**
 * 定义部门的Reducer函数，
 * @param {saate} state
 * @param {action} action
 * @returns
 */
const departmentReducer = (state = departmentState, action) => {
  return state;
};
export default departmentReducer;
